{
  "name": "brave-search-mcp-railway",
  "version": "1.0.0",
  "description": "Brave Search MCP Server deployed on Railway",
  "type": "module",
  "scripts": {
    "start": "npx -y @brave/brave-search-mcp-server --brave-api-key $BRAVE_API_KEY --transport http --port ${PORT:-8080} --host 0.0.0.0"
  },
  "dependencies": {}
}
