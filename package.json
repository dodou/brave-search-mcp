{
  "name": "brave-search-mcp-railway",
  "version": "1.0.0",
  "description": "Brave Search MCP Server deployed on Railway",
  "type": "module",
  "scripts": {
    "start": "BRAVE_API_KEY=$BRAVE_API_KEY npx -y @brave/brave-search-mcp-server --transport http --port ${PORT:-8080} --host 0.0.0.0"
  },
  "dependencies": {}
}
